name: Deploy Full Stack 🚀

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy Backend + Frontend to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22

          script: |
            echo "📂 Loyihaga o‘tyapman..."
            cd /root/mconsultwebsite

            echo "📥 Git pull..."
            git pull origin main

            echo "🔧 BACKEND DEPLOY"
            cd backend
            npm install
            npm run build
            pm2 restart mconsult-backend || pm2 start dist/main.js --name mconsult-backend

            echo "🎨 FRONTEND DEPLOY"
            cd ../frontend

            # static html bo‘lgani uchun build yo‘q
            sudo nginx -t
            sudo systemctl reload nginx

            echo "✅ FRONTEND + BACKEND DEPLOY TUGADI"
